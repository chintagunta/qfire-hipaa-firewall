# Errors

Raised only at load time (parsing rules/chains); {py:func}`~qfire.evaluate` never raises
for a detector-level failure — see {doc}`../concepts` ("Fail-closed").

```{eval-rst}
.. automodule:: qfire.errors
   :members:
```
